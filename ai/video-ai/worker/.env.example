CAMERA_ID=c200
# 기본값: MediaMTX가 재중계하는 RTSP 소스 사용(카메라 대신 게이트웨이로 부하 집중)
RTSP_INPUT=rtsp://hls-gateway:8554/c200

# 분석 FPS와 민감도 설정
AI_FPS=8
SENSITIVITY=0.015   # 프레임 대비 움직임 비율(0.0~1.0)
MIN_ACTIVE_FRAMES=6 # 연속 프레임 임계

# 클립 저장
CLIP_DIR=/app/clips
POST_SEC=5
